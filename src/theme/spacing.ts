export const spacing = {
  x1: 8,
  x2: 16,
  x3: 24,
  x4: 32,
  x5: 40,
  /** Cards, sheets and list rows. */
  radius: 12,
  /** The wallpaper card the deck hands the reader. */
  radiusLarge: 26,
  pill: 9999,
  control: 48,
  /** Height of the deck tab bar, matching the board. */
  tabBar: 64,
} as const;
